# IS-EMRI-o86-A3 — CEVAP

1. `IZIN_PRE`/`IZIN_POST` `SyncCommandHandler.cs`'te (`IsAuthorizedAsync`'in Task dalı çağırır): `IzinPreAsync(preScope, isNewEntity, entityId, actorId, ct)` — `isNewEntity` ise `true`; `preScope is null` ise `_store.IsTaskOwnerAsync(entityId, actorId, ct)`; `preScope is P` ise `IsProjectOwnerOrMemberAsync(P, actorId, ct)`. `IzinPostAsync(postScope, actorId, ct)` — `postScope is null` ise **koşulsuz** `true` (asimetri, DOKUNMA LİSTESİ), `postScope is Q` ise `IsProjectOwnerOrMemberAsync(Q, actorId, ct)`. `ISyncStore.IsTaskOwnerAsync` (`SyncStore.cs`): `SELECT EXISTS (SELECT 1 FROM tasks WHERE entity_id = @t AND owner_id = @a)` — FAIL-CLOSED: `tasks` satırı yoksa (materyalizasyon boşluğu) `EXISTS` `false` döner, yetki AÇMAZ.
2. Doğruluk tablosu **15 satır**, tek `[Theory]` (`Dogruluk_tablosu_IZIN_PRE_IZIN_POST_H8`, mevcut `D9OwnerIdVisibilityTests.cs` dosyasında — yeni dosya açılmadı). Eksenler: preCase (5 değer: `Yeni`, `PreNullBenim`, `PreNullBaskasinin`, `PreScopeUyeyim`, `PreScopeUyeDegilim` — `isNew` bu eksenin İÇİNE gömülü, çünkü yeni varlıkta preScope anlamsızdır) × postCase (3 değer: `PostNull`, `PostSahibim`, `PostUyeDegilim`). Dışarıda bırakılan bileşim: **"Yeni" + gerçek preScope değeri** (4 preCase × 1 "Yeni" kombinasyonu, yani preScope'un ayrı ayrı `null-benim`/`null-başkasının`/`P-üyesiyim`/`P-değilim` ile "Yeni" birleşimi) — yeni bir varlığın hiç geçmiş bağlamı (hidrasyonu) yoktur, `IZIN_PRE` bu durumda `preScope`'u hiç SORMAZ (kod: `if (isNewEntity) return true;`, preScope parametresi kullanılmadan döner) — bileşim çalıştırılabilir değil, anlamsız. Bu yüzden "Yeni" tek başına yalnız postCase'in 3 değeriyle test edilir (3 satır) + kalan 4 preCase × 3 postCase tam çarpım (12 satır) = 15.
3. **Mutant 1** (`IZIN_PRE`'in null kolu, `IsTaskOwnerAsync` çağrısı koşulsuz `true`'ya indirgendi): `PreNullBaskasinin`×`PostNull` ve `PreNullBaskasinin`×`PostSahibim` kırmızı (2/15) — `PreNullBaskasinin`×`PostUyeDegilim` kırmızı DEĞİL (zaten `IZIN_POST` tek başına reddediyor, mutant'a duyarsız). **Mutant 2** (`IZIN_PRE`'in scope kolu, `IsProjectOwnerOrMemberAsync(preScope)` koşulsuz `true`): `PreScopeUyeDegilim`×`PostNull` ve `PreScopeUyeDegilim`×`PostSahibim` kırmızı (2/15). **Mutant 3** (`IZIN_POST`'un scope kolu, `IzinPostAsync` koşulsuz `true`): `Yeni`×`PostUyeDegilim`, `PreNullBenim`×`PostUyeDegilim`, `PreScopeUyeyim`×`PostUyeDegilim` kırmızı (3/15). Üçü de ham çıktı — [01-mutantlar-kirmizi.txt](01-mutantlar-kirmizi.txt).
4. 🔴 **Beyan edilmiş sınır** (düzeltme bu emrin işi DEĞİL, README'ye de yazılır): `tasks.owner_id` `ON CONFLICT`ta güncellenmez — **ilk yazan sabit kalır**. Bir üye P içinde bir görev yaratırsa (`tasks.owner_id`=o üye) ve proje sahibi sonradan o görevi P'den koparırsa (`projectId:null`), görev üyenin KENDİ Gelen Kutusu'na düşer ve **artık sahip bile o göreve IsTaskOwnerAsync üzerinden yazamaz** (sahip değil, üye materyalize sahiptir) — sahiplik devri op'u kapsam dışı, rol kademesi eklenmedi.
5. `verify.ps1` **EXIT:0** (ham satır — [04-verify-ps1.txt](04-verify-ps1.txt), CVE gate temiz, `== VERIFY PASSED ==`). Test sayısı: o86-A2 sonrası 160 → o86-A3 sonrası **175** (160 + 15 yeni `[InlineData]` satırı, tek `[Theory]`). `git status --porcelain -- src tests` içinde `src/client/**` **YOK** (yalnız backend + test dosyaları) — aşağıda ham çıktı:
```
 M src/backend/Momentum.Application/Abstractions/Sync/ISyncStore.cs
 M src/backend/Momentum.Application/Features/Sync/SyncCommandHandler.cs
 M src/backend/Momentum.Infrastructure/Sync/SyncStore.cs
 M tests/Momentum.Persistence.Tests/D9OwnerIdVisibilityTests.cs
```
