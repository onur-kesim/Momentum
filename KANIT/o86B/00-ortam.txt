IS-EMRI-o86-B §F -- ORTAM OLCUMU (araclar varsayilmadi, olculdu)
=====================================================================

adb PATH'te YOK, %LOCALAPPDATA%\Android\Sdk\platform-tools altinda da YOK
(find: 'C:\Users\gulci\AppData\Local\Android\Sdk\platform-tools': No such
file or directory). Gercek Android SDK konumu:

  ANDROID_HOME = D:\Android\Sdk
  adb          = D:\Android\Sdk\platform-tools\adb.exe   (Version 37.0.1-15733141)
  emulator     = D:\Android\Sdk\emulator\emulator.exe     (version 37.1.11.0)
  AVD dizini   = D:\Android\avd
  cmdline-tools/avdmanager = D:\Android\Sdk\cmdline-tools\latest\bin\avdmanager.bat
  JAVA_HOME (avdmanager icin) = C:\Program Files\Android\Android Studio\jbr

Mevcut AVD: tuzak_api34 (android-34, google_apis, x86_64, pixel_5 ekran
1080x2340/440dpi). Bu is emri sirasinda ikinci bir AVD (tuzak_api34_b)
denendi ama commit-limit kisitindan (asagida) dolayi guvenilir acilamadi --
K-o88/2'nin ruhuna uygun sekilde GERCEK TELEFON + tek emulatore (tuzak_api34)
dusuldu (Onur kilidi, 4-5 Eyl).

PAGEFILE / COMMIT LIMITI OLCUMU (buyutulmedi -- artik GEREKSIZ, adb reverse
firewall/admin duvarini atlar):
  ONCE: TotalVirtualMemorySize=27053304 KB (~25.80 GB) · TotalVisibleMemorySize=
  20761848 KB (~19.80 GB fiziksel) · FreePhysicalMemory=7134596 KB (~6.80 GB bos) ·
  pagefile C:\pagefile.sys SABIT 6144/6144 MB · pagefile tepe kullanim 443 MB.
  Iki emulator aynı anda acilmaya calisilinca "Insufficient RAM" (Needed:
  2560.00 MB, Remaining: ~585-1283 MB commit payi) hatasi ALINDI; RAM'i
  1024M'a dusurunce bu sefer emulator 6+ dakika ilerlemeden (bellek kullanimi
  SABIT kalarak) SIKISTI -- bu ortamda iki emulatoru AYNI ANDA guvenilir
  calistirmak MUMKUN DEGIL (fiziksel RAM'de bosluk var ama commit limiti
  kisitli). PAGEFILE BUYUTULMEDI (Onur kilidi: gerekci degil artik, kendi
  vaktinde buyutecek).

BACKEND: `dotnet run` ile HOST'ta calisiyor (ASPNETCORE_ENVIRONMENT=Development,
ConnectionStrings__Momentum=Host=localhost;Port=5432;..., 0.0.0.0:5298 dinliyor).
/health/ready 200 dondu (asagida ham cikti).

ULASIM YONTEMI (Onur kilidi, adb reverse -- LAN/firewall DEGIL): backend
0.0.0.0:5298 dinliyor ama Windows Firewall gelen kurali OLCULEMEDI (okumak
bile admin istedi) ve pagefile ile ayni admin duvarina carpar. `adb reverse
tcp:5298 tcp:5298` USB tuneli firewall'u ATLAR, admin ISTEMEZ. Bu yuzden TEK
APK `SENKRON_SUNUCU_URL=http://localhost:5298` ile derlendi (10.0.2.2 KULLANILMADI --
o adres iki cihazi ayirirdi, tek adres ikisine de HEM emulatore HEM gercek
telefona ayni sekilde uygulanir, adb reverse ikisinde de ayri ayri kurulur).

SINIR 29 OLCUMU (varsayilmadi -- POST /v1/sync basliklarindan olculdu, 4-5 Eyl):
  Test hesabi kaydedildi (POST /v1/auth/register -> 201, userId+accessToken alindi).
  A) SADECE "Authorization: Bearer <JWT>" (X-Momentum-Dev-User YOK)      -> HTTP 200
  B) SADECE "X-Momentum-Dev-User: <userId>" (Authorization YOK)          -> HTTP 200
  C) HICBIRI (basliksiz)                                                 -> HTTP 401
  SONUC: Development profilinde IKI YOL DA AKTIF -- X-Momentum-Dev-User
  "DEVRE DISI" DEGIL. README/main.dart'in "gercek JWT girisinde bu yol
  devre disi" notu URETIM (Production) profiline aittir, Development'a
  degil (bu olcum Development'ta yapildi, backend boyle baslatildi).
  Pratikte fark etmiyor: HttpSenkronAgi HER ZAMAN X-Momentum-Dev-User
  basligini da gonderir (actorId=ayarlar.devUserId), ama bu deger ARTIK
  GERCEK giris yapan kullanicinin userId'siyle SENKRONIZE (ayarlariHazirla'nin
  "ezme" mekanizmasi sayesinde) -- iki yol AYNI kimlige isaret eder,
  cakisma yoktur.

CANLI TUR ONCESI YENIDEN-OLCUM (5 Eyl, backend+emulator surecleri onceki
oturumda dusmustu -- varsayilmadi, sifirdan olculdu):
  - Telefon (fba69c15, flourite_tr): ilk `adb devices` "unauthorized" -- ekranda
    "Bu bilgisayardan hata ayiklamaya izin ver" onaylandiktan sonra "device".
  - Backend (`dotnet run`, host, 0.0.0.0:5298) sureci ayakta degildi (tasklist
    bos) -> yeniden baslatildi, /health/ready host'ta 200 dondu.
  - Emulator (tuzak_api34/emulator-5554) da ayakta degildi -> yeniden acildi,
    sys.boot_completed=1 ile boot tamamlandi.
  - TELEFON negatif/pozitif kontrol (reverse KURULMADAN ONCE / SONRA):
      ONCE: curl http://localhost:5298/health/ready -> "Failed to connect... (7)"
      reverse: `adb -s fba69c15 reverse tcp:5298 tcp:5298`
      SONRA: curl http://localhost:5298/health/ready -> HTTP_200
  - EMULATOR negatif/pozitif kontrol (curl yok bu imajda, nc ile olculdu):
      ONCE: nc -w3 localhost 5298 -> "Connection refused" (exit 1)
      reverse: `adb -s emulator-5554 reverse tcp:5298 tcp:5298`
      SONRA: nc ile ham HTTP istegi -> "HTTP/1.1 200 OK ... Server: Kestrel" (exit 0)
  - APK (build/app/outputs/flutter-apk/app-release.apk, 5 Eyl 01:56, localhost:5298
    dart-define'li derleme) her iki cihaza da `install -r` ile YENIDEN kuruldu
    (paket her ikisinde de onceden mevcuttu ama surum farki riskini olcmemek
    yerine tazelendi) -- ikisi de "Success".
