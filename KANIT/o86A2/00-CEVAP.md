# IS-EMRI-o86-A2 — CEVAP

1. `project_access` iki kolonu da index'e düşer: `projects.owner_id` → `ix_projects_owner_deleted_pos_entity`,
   `project_members.user_id` → `ix_project_members_user_project` (Bitmap Index Scan, Seq Scan yok — [01-explain-view.txt](01-explain-view.txt)).
   4. bulgu düzeltmesinin yeni `tasks.project_id IN (project_access)` deseni Seq Scan'e düştü (208 satırlık küçük tabloda doğru plan) — §A kuralınca dar bir indeks (`ix_tasks_project_id`, migration `AddTasksProjectIdIndex`) eklendi, izole ölçümde (`enable_seqscan=off`) planlayıcının onu seçtiği doğrulandı.
2. Pull artık üç kollu `WHERE (owner_id=@actor OR scope_id IN project_access(@actor) OR old_scope_id IN project_access(@actor))` — `project_access` görünümü üzerinden (`SyncPuller.cs`); sahip kendi projesinin `scope:{P}` hub grubuna katılır (H2, `ScopeMembershipSource.GetScopesAsync`), `project_members`'a hiç yazılmaz (§C3).
3. Yazma yetkisi `SyncCommandHandler.IsAuthorizedAsync`'te: Task dalı `KABUL ⟺ IZIN(preScope) VE IZIN(postScope)`, `IZIN(null)=true` — yeni-varlık istisnası ayrı kod dalı DEĞİL, preScope'un doğal null'luğundan kendiliğinden çıkar (§D). 4. bulgu düzeltmesi `SyncPuller.cs`'te iki ayrı yerde: artımlı kanalda `owner_id` kolu artık YALNIZ `scope_id IS NULL AND old_scope_id IS NULL` iken geçerli; snapshot kanalında (`ReadOwnedEntitiesAsync`) Task/Project artık outbox geçmişi değil GÜNCEL materyalize durum (`tasks.project_id`/`owner_id`, `project_access`) üzerinden okunur.
4. H1'in artımlılığı: canlı tur adım 7 A'nın `a_cursor`'ı (adım 2 sonu, T'nin yaratımından hemen sonraki horizon, `{"xid":...,"seq":0}` — null DEĞİL) ile gerçek bir `sinceCursor` göndererek pull yapar; TAZE snapshot (`sinceCursor:null`) KULLANILMAZ — o86-A'yı yanlışlıkla geçirmiş olan tam buydu ([04-canli-tur-o86a2.txt](04-canli-tur-o86a2.txt) adım 7b).
5. §G kapısı (`ArchitectureRuleTests.Rule5_...`): `SyncPuller.cs`/`ScopeMembershipSource.cs`'in kaynak METNİNDE literal `project_members` GEÇEMEZ. Mutant `ScopeMembershipSource.cs`'e bu literali geri koydu → test kırmızı ("scopeMembershipSource should not contain project_members but was actually...") — [03-mutantlar-kirmizi.txt](03-mutantlar-kirmizi.txt).
6. `verify.ps1` EXIT 0 (build 0 uyarı/0 hata, CVE gate temiz — [06-verify-ps1.txt](06-verify-ps1.txt)). Test sayısı: o86-A sonrası 151 → o86-A2 sonrası **160** (H1,H2,H3,H4,H5,H6,§G,H7a,H7b = 9 yeni `[Fact]`). `git status --porcelain -- src tests` içinde `src/client/**` YOK (yalnız backend + test dosyaları).
7. **4. bulgu (Onur kilidi, iş emrinin orijinal §A-§E/H1-H6 kapsamının DIŞINDA, canlı tur adım 9'da bulundu):** eski üye, üyeyken yazdığı KENDİ (scope taşıyan) outbox satırı `owner_id` koluyla eşleştiği için çıkarıldıktan SONRA bile o varlığı SONSUZA DEK görüyordu — DB'de doğrulandı, sonra HEM artımlı HEM snapshot kanalında AYRI AYRI kapatıldı: artımlı `owner_id` kolu artık yalnız scope'suz satırlarda geçerli (H7a), snapshot artık outbox geçmişi değil güncel `tasks`/`projects` durumu okur (H7b, Onur'un işaret ettiği "önce kişisel yaratıp sonra projeye taşıma" senaryosuyla sınandı) — ikisi de ayrı mutant, ham kırmızı çıktı ile kanıtlandı; `ScopeMembershipSource.cs`'in "anında yansır" yorumu yalnız kendi (hub grubu) sorgusu için olduğu netleştirildi.
